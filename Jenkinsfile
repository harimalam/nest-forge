pipeline {
    agent { label 'built_in' }

    stages {
        stage('Log Only') {
            steps {
                echo "CI/CD pipeline triggered — no actions performed."
            }
        }
    }
}